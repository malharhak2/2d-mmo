define ([], function () {
	var config = {
		player : {
			speed : 200
		},
		brute_force : false,
		bufferSize : 2,
		interLerping : true,
		interLerp : 10,
		debug : true,
		lagOffset: 100,
	};

	return config;
});